###############################################################
#                         Users Database                      #
###############################################################

# This file can be used if you do not have an LDAP set up.

# List of users
users:
  {{ USERNAME }}:
    disabled: false
    displayname: "{{ NAME }}"
    email: "{{ EMAIL }}"
    password: {{ HASHED_PASSWORD }}
    groups:
      - admins